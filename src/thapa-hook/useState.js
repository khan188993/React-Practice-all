import React from 'react'
import '../App.module.css' 
//local will work if import addded 
//in react js basic click will not work
//hook must see in console.log 
//react.useState 

const UseState = () => {
    return (
        <div>
            <h1>Thapa UseState</h1>
        </div>
    )
}

export default UseState
